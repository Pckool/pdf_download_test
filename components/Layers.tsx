'use client'

import React from 'react'
import { Toaster } from 'sonner'

const Layers = () => {
  return (
	<div>
		{/* A layer for toast notifications */}
		<Toaster />
	</div>
  )
}

export default Layers